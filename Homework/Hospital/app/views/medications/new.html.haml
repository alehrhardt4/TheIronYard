%script{:src => "/assets/medication.js", :type => "text/javascript"}
= form_for [@hospital, @patient, @medication] do |f|
  / - if  f.object.errors.messages.length > 0
  /   - f.object.errors.messages.inspect
  /   %br/
  /   - f.object.errors.messages.each do |key, value|
  /     - value.each do |message|
  /       = key.capitalize
  /       = message
  /       %br/
  /     %br/
  = f.label :name
  = f.text_field :name
  %br/
  = f.label :description
  = f.text_field :description
  %br/
  = f.submit "Submit", disabled: "disabled"
  =link_to "Back", hospital_patient_medications_path